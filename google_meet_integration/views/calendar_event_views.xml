<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="calendar_event_new_view_form" model="ir.ui.view">
        <field name="name">calendar.event.new.view.form</field>
        <field name="model">calendar.event</field>
        <field name="inherit_id" ref="calendar.view_calendar_event_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="before">
                <group>
                    <button attrs="{'invisible':[('videocall_location','=',False)]}" class="button_box oe_right btn btn-primary" name="join_meeting" string="Join Meeting" type="object"/>
                </group>
            </xpath>
            <xpath expr="//page[@name='page_invitations']" position="after">
                <group>
                    <group string="Google Meet">
                        <field name="genrate_google_meet_link"/>
                        <field name="calender_id_hpl" invisible="1"/>
                        <field name="event_id_hpl" invisible="1"/>
                    </group>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
